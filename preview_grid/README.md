# preview_grid

Small local `react-vite` page for quickly testing `glide-data-grid` from this repository source.

## Run

From repository root:

```bash
npm run dev --prefix preview_grid
```

Open:

`http://localhost:5174`

## Notes

- Uses local source import: `../packages/core/src`.
- Does not require `packages/core/dist`.
